#include <stdio.h>

#include "point.h"

Point* charToPoints(char c) {
    FILE *fptr;
    char filename[] = {c, '.', 't', 'x', 't'};

    //fptr = fopen(filename, 'rb');

	Point p[] = {{1,2}};
    return(p);
    
}